# .env configuration
# Notes:
# - Values are read as strings unless noted.
# - Defaults shown below are the script's internal defaults when a variable is unset.
# - Copy this file to '.env' and adjust values for your environment.

# =====================
# AI Provider
# =====================
# AI provider API URL (string). Default: https://openrouter.ai
#AI_API_URL=https://openrouter.ai
# AI provider API key (string). No default; REQUIRED.
AI_API_KEY=your-api-key-here
# Model identifier (string). Default: openai/gpt-5-mini
# You can change this to any compatible model available to your key.
#AI_API_MODEL=google/gemini-2.5-pro
# Optional: Maximum Tokens used during one AI call. Default: 10000
#AI_API_MAX_TOKENS=2000
# Optional: URL of your app for attribution (string). Default: http://localhost
#APP_URL=http://localhost
# Optional: short title for your app (string). Default: Delta Chat AI Bot
#APP_TITLE=Delta Chat AI Bot

#======================
# General Configuration
#======================
# Optional: System Prompt used to instruct the bot.
# Default: You are a helpful chatbot that answers the user's messages with short, direct replies. Keep your answers clear, factual, and concise.
#SYSTEM_PROMPT=
# Optional: Maximum length of User Input in characters. Longer messages will be truncated.
# Default: 1000
#MAX_MESSAGE_LENGTH=

# =====================
# Sender whitelist
# =====================
# Comma-separated list of allowed senders this bot is allowed to respond to.
# No default; REQUIRED.
RESPOND_TO=user1@example.com,user2@example.com
